#include <stdio.h>

#define LINHAS 10
#define COLUNAS 10

int main() {
    int tabuleiro[10] = {'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J'};
    int matriz [LINHAS] [COLUNAS];

    int tabuleiro[10][10];
    for (int i = 0; i < LINHAS; i++){
        for(int j = 0; j < COLUNAS; j++)
    }
    
    
    return 0;
}
